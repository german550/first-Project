
<% @descriptions.each do |description| %>
    <%= description.description%><br>
  <%= image_tag(description.image) %>
  <div class="col-1">
    <%= button_to "delete", "/professions/#{description.profession_id}/descriptions/#{description.id}", method: :delete %>
  </div>
<% end %>
